package com.converter.service;

public class SourceService {

    // basic retrieval of data
    // create youtube and spotify implementations
    // use processbuilder
    // in this case we don't even access our own repo
    // (although a feature to save and name playlists would be good)
    // also csv folder should be in a resources folder
    
}
